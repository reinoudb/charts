apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fail2ban-prometheus-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "fail2ban-prometheus-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fail2ban-prometheus-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
