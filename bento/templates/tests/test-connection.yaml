apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bento.fullname" . }}-test-connection"
  labels:
    {{- include "bento.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bento.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
