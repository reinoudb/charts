apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kopia.fullname" . }}-test-connection"
  labels:
    {{- include "kopia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kopia.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
