apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "urbackup.fullname" . }}-test-connection"
  labels:
    {{- include "urbackup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "urbackup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
