apiVersion: v1
kind: Service
metadata:
  name: {{ include "restic-server.fullname" . }}
  labels:
    {{- include "restic-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restic-server.selectorLabels" . | nindent 4 }}
